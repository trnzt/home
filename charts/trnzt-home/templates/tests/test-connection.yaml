apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trnzt-home.fullname" . }}-test-connection"
  labels:
    {{- include "trnzt-home.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trnzt-home.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
