apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trnzt-com.fullname" . }}-test-connection"
  labels:
    {{- include "trnzt-com.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trnzt-com.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
